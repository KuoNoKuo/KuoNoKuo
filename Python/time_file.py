import time 
#Used to set delays and clock
print(0)
time.sleep(1) # '.sleep' = wait | parameters = seconds to wait
print(1)

print(time.localtime()) # '.localtime' is used to get the current time | The output =list
